---
name: Docent build progress
description: Current step, completed steps, and what's next; read first on every Docent session
type: project
---

Docent is a Python CLI "control center" for grad-school workflows. Design lives in `Docent_Architecture.md` (committed to repo). Build order is strict — do not reorder.

## Status as of 2026-05-06 (Reading tool rewrite shipped; Step 12 next — `~/.docent/plugins/` discovery)

- [x] 1. Project skeleton + `docent --version`
- [x] 2. Config loader + Rich console singleton
- [x] 3. Tool base class + registry + `@register_tool` + `Context`
- [x] 4. Dynamic Typer commands + `docent list` / `docent info`
- [x] 5. litellm wrapper (`LLMClient`, lazy-imported, `Context.llm`)
- [x] 6. Subprocess executor (`Executor`, `Context.executor`)
- [x] 7a. Multi-action contract extension (`@action` decorator)
- [x] 7b. First real tool: `paper add` stub (validates the contract end-to-end)
- [x] 7c. `docent.learning.RunLog` (per-namespace JSONL run-log; cap-and-roll; not on `Context`)
- [x] 8. Simple paper CRUD actions (next/show/search/stats/remove/edit/done/ready-to-read/mark-keeping/export); `add` integrated with `RunLog`
- [x] 9. PDF-driven `paper add` (DOI→CrossRef→PDF-DOI→CrossRef→PDF-info→filename fallback) + new `paper scan --folder` action; `process_inbox` superseded by `scan`. **Real-data validated 2026-04-26**: tested against a real DOI, CrossRef metadata fetched correctly.
- [x] 10. Progress streaming extension (`ProgressEvent`, generator-based actions, CLI Rich progress; only `paper scan` migrated). **Real-data validated 2026-04-26**: scanned a folder mixing PDFs + a .docx + a duplicate; .docx ignored, duplicate detected and skipped, progress bar streamed.
- [x] 10.5. Paper config (`paper.database_dir`, `paper.mendeley_watch_subdir`); `paper config-show` / `config-set`; first-run prompt; `scan --folder` defaults to configured DB. **Real-data validated 2026-04-26 after two bug fixes**: prompt-eaten-by-Rich-Live (split `scan` into resolver + generator) and quote-wrapped-paths corrupting config (strip quotes in `prompt_for_path`, validate `is_dir()` before `write_setting`). Both branches of the first-run prompt (cancel / create / custom path) now work.
- [x] 10.6. pytest harness — `[dependency-groups] dev`, `tests/` (conftest + 6 files, 30 tests), `~0.6s` runtime. Covers registry contract, RunLog round-trip + cap-and-roll, `write_setting` TOML round-trip, `prompt_for_path` quote-stripping (Step 10.5 regression), queue add + collision, metadata fallback chain. ruff + Typer `CliRunner` deferred. Found + fixed `_filename_heuristic` underscore-year bug (run year regex against normalized title, not raw stem) — Mendeley-style `Smith_2019_topic.pdf` now extracts year correctly.
- [x] 10.7. `PaperQueueStore` extracted to `src/docent/tools/paper_store.py` (107 LOC). Owns persistence + state recompute (`load_queue` / `load_index` / `save_queue` / `banner_counts`); self-initializes the directory on first write and returns safe defaults on missing files. `paper.py` shrank 887 → 799 LOC. Step 11 sync ops will mutate via `self._store` rather than scattered helpers. New `tests/test_queue_store.py` (7 unit tests, no PaperPipeline) — full suite 37 green in ~0.6s. UI leak (`docent.ui` import in `paper.py`) NOT addressed; next micro-step if it bites.
- [x] 10.8. UI-leak cleanup. `_require_database_dir` now returns `tuple[Path | None, str | None]` (path or error message) — caller folds it into `ScanResult.message`. Dropped `from docent.ui import get_console` from `paper.py`. Trade-off: rare "user typed non-existent path" branch loses Rich markup, becomes plain text in the result. New `tests/test_database_dir.py` (2 tests). Suite 39 green.
- [x] 11.1. `sync-status` action — local cross-tab queue × `database_dir` × `mendeley_watch_subdir`. Buckets: `in_queue_with_file` / `in_queue_missing_file` / `orphan_pdfs` / `promotable` / `in_watch`. New `PaperQueueStore.list_database_pdfs()` static helper (the Step-10.7-flagged filesystem-counts hook). New `SyncStatusResult` BaseModel; default repr ships (no renderer abstraction). `tests/test_sync_status.py` (8 tests). Suite 47 green. **Real-data validated 2026-04-30**: ran against user's actual Paper Database, surfaced 5 matched / 2 missing / 0 orphans. **Deviations from `paper_pipeline_port_plan.md` Step 11**: (a) single-shot, not generator (sub-100ms with no slow phase; revisit when slow phase lands), (b) no Semantic Scholar orphan identification (deferred to a `--identify-orphans` flag).
- [x] 11.2. `sync-pull` (Unpaywall via executor + curl) — generator action; DOI direct path + CrossRef bibliographic title-search fallback persists resolved DOI; `--id` / auto-mode / `--dry-run`; closed-access surfaces `doi_url` + `journal` in `no_oa` bucket so user can check institutional access. New `unpaywall_email` setting (hard-required, configurable via `config-set`). Helpers: `_unpaywall_lookup`, `_crossref_title_search`, `_download_pdf` (curl `-o`, no magic-byte sniff). In-place queue mutation tracked by `mutated_ids` set; reconciled against fresh load before save. New `tests/test_sync_pull.py` (9 tests, FakeExecutor pattern-matches curl args). Suite 47 → 56 green in ~1.4s. **Real-data validated 2026-04-30**: queued an OA DOI, `sync-pull` wrote a real PDF — happy path green. Three edge-case bugs landed in fix batch `5584a6a` (CrossRef fuzzy guard, institutional-access hint, PDF Content-Type + magic-byte validation, plus `queue-clear --yes`). Identifier-free adds dropped in `380ebc5` (QueueEntry validator + `add` requires --pdf or --doi). Re-validated on real data after both. Suite 65 → 63 green (net: -3 obsolete + 1 new).
- [x] 11.3. `sync-promote` (DB → Watch **move**, not copy — user preference: no duplicate PDFs lying around). Generator action; auto mode (kept + has-file + not promoted + filename not in Watch) and `--id` override; `--dry-run`. New `QueueEntry.promoted_at` field. New `paper_sync.move_to_watch()` (`shutil.move`, raises on collision). Two heal branches: `pdf_path` already inside Watch → set `promoted_at` only; `pdf_path` rotted but filename in Watch → repoint `pdf_path` + set `promoted_at`. `sync-status.promotable` updated to AND both signals (`promoted_at is None` AND filename not in Watch). Buckets: `promoted` / `already_promoted` / `healed` / `missing_file` / `not_eligible` / `failed` / `dry_run_promote`. New `tests/test_sync_promote.py` (10 tests) + 1 sync-status regression. Suite 63 → 74 green in ~2.9s. **Real-data validated 2026-05-01**: all real-world tests pass.
- [x] 11.4. `sync-mendeley` + minimal MCP-client adapter. **In-process MCP client chosen** (executor seam was wrong for stateful protocol — see decisions.md 2026-05-01). New `src/docent/tools/mendeley_client.py` (~120 LOC, lazy-imports `mcp` SDK, sync facade `lookup_doi` / `search_library` returning `{"items": [...], "error": str | None}`). `mcp>=1.0,<2` dep added. Generator action with auto mode (`promoted_at is not None` AND `mendeley_id is None`), `--id` override, `--dry-run`. Buckets: `linked` / `already_linked` / `not_found` / `ambiguous` / `not_eligible` / `failed` / `dry_run_link`. Auth-shaped failures append `mendeley-auth login` hint to summary. New `QueueEntry.mendeley_id` field. New `PaperSettings.mendeley_mcp_command` (defaults to `["uvx", "mendeley-mcp"]`; not yet exposed via `config-set` — list-typed, defer). 12 new tests; suite 74 → 86 green in ~12s. **Real-data fully validated 2026-05-01**: (a) dry-run against live Mendeley MCP — auth ok, library search returned 5 candidates for the one no-DOI promoted entry, bucketed as `ambiguous`; (b) `--id chaiwongsaen-2019-morphological` real run — DOI hit, catalog lookup returned `catalog_id`, persisted to `queue.json`. **Three fixes during validation**: (1) `_extract_mendeley_id` now also checks `catalog_id` (`mendeley_get_by_doi` returns catalog items, not library docs); (2) `mendeley_client._normalize_doi` strips `https://doi.org/` prefix before sending — caught when chaiwongsaen entry's URL-form DOI returned no match; (3) global `docent` install (via `uv tool install`) doesn't auto-pick-up the new `mcp` dep — use `uv run docent ...` from project dir, or `uv tool install --reinstall --editable .`. Cold-call latency ~5.3s (subprocess + OAuth + Mendeley GET); pooling deferred.
- [~] 11.5. PDF metadata extraction improvements. **Reverted-in-spirit 2026-05-02 — real-data validation failed (most PDFs ended up with the author list as the title; some concatenated section headings + authors). Code stays in tree until Step 11.8 deletes it so we have one clean diff. See `decisions.md` 2026-05-02 + `paper_pipeline_port_plan.md` Step 11.6+ for the pivot.** Original change set: Three changes to `paper_metadata.py`: (a) DOI regex now scans up to 20 pages (was 5; many journals only print the DOI in last-page footer), (b) new `_extract_title_by_font_size(reader)` heuristic picks the largest-font-size text run on page 1 (≥10 chars, skips DOI/URL/© leaders) when the PDF info dict lacks `/Title`, (c) new `crossref_title_search(title, executor, threshold=0.92)` queries CrossRef `query.bibliographic` and accepts the best of top-5 only when `difflib.SequenceMatcher` ratio ≥ 0.92 — this is the helper the 11.2 batch reverted, brought back with a strict fuzzy guard so it can't return wrong papers. New chain: `doi-crossref → pdf-doi-crossref(20pg) → pdf-title-crossref → pdf-metadata → filename`. Author extraction from "text under title" deliberately skipped — once we land a CrossRef title-search hit we get authoritative authors for free. **Scope cuts vs original plan:** dropped `--reextract` flag (user will wipe queue + re-scan rather than upgrade-in-place; ids would otherwise rename and cascade through sync state). 8 new tests (`test_metadata_resolver.py`: 6 → 14): title-search fuzzy accept, fuzzy reject, empty query, font-size largest-run pick, font-size skip-too-short, font-size empty-page, plus two new resolve_metadata flow tests. Suite 86 → 94 green in ~11s.
- [x] 11.6. `sync-from-mendeley` action — generator (`discover` → `reconcile`). New `paper.queue_collection` setting (default `"Docent-Queue"`); plumbed through `config-show`/`config-set`/`_KNOWN_PAPER_KEYS`. New `mendeley_client.list_folders()` + `list_documents(folder_id, limit=200)` sync facades; `list_documents` takes `folder_id` not name (live-MCP probe), so the action makes one extra `list_folders` call per run to resolve the configured name. **Ambiguity & error paths:** missing collection / duplicate name / `list_folders` transport error / `list_documents` error / per-doc missing-id are all bucketed with actionable hints. Buckets: `added`/`unchanged`/`removed`/`failed` (+ `dry_run_added`/`dry_run_removed`). New entries snapshot `title`/`authors`/`year`/`doi` from the `list_documents` payload (no extra round-trip — data's in-hand) so existing `next`/`show`/`stats`/`search` keep working unchanged; replacing the snapshot with read-through Mendeley reads is the genuine 11.7 follow-up. New `_build_entry_from_mendeley` + `_normalize_mendeley_authors` helpers. **Validator relaxed:** `_require_pdf_or_doi` → `_require_identifier` — `mendeley_id` is now a third valid identifier (Mendeley-keyed entries are persistable without DOI/PDF). 23 new tests in `test_sync_from_mendeley.py`; suite 94 → 117 green in ~12s. **Real-data validated 2026-05-02:** `uv run docent paper sync-from-mendeley` against live Mendeley library — OAuth refresh + folders GET succeeded, "Docent-Queue not found" actionable hint surfaced exactly as designed. **Deviations from plan:** (a) used `status="queued"` not plan's `"to-read"` (existing `next`/`stats` filter on `"queued"`; new label is for the 11.10 schema flip), (b) deferred `MendeleyCache` scaffolding to 11.7 where it has a consumer (was originally bundled per Q1 discussion; I narrowed scope mid-implementation — see decisions.md 2026-05-02 entry).
- [x] 11.7. Read-through Mendeley metadata cache. **File-backed** (not in-process per the original plan — across-CLI persistence is the whole point; one-shot Python invocations would otherwise burn the cache every call). New `src/docent/tools/mendeley_cache.py` (~120 LOC): `MendeleyCache.get_collection(folder_id)` returns `{mendeley_id: doc}` from disk if fresh (TTL 300s), else fetches via `mendeley_list_documents` and writes back. Atomic temp+rename. Cache lives at `<cache_dir>/paper/mendeley_collection.json`. Wraps `list_documents` only — `get_document` deferred until a reader needs the fields it adds (abstract / attachments). New paper.py helpers: `_resolve_collection_folder_id_quiet` (silent reader-side variant of sync's verbose folder-resolver), `_load_mendeley_overlay`, `_overlay_entry`, `_apply_overlay`. `next` / `show` / `search` overlay fresh title/authors/year/doi over the queue.json snapshot before responding; on transport / auth / collection-missing, the snapshot stands unchanged. **`stats` deliberately skipped** — none of its outputs are Mendeley-owned (status / priority / course / keeping), so a cache call would be wasted MCP traffic. `sync-from-mendeley` calls `cache.invalidate(folder_id)` after a successful (non-dry) write so the next reader pulls fresh data. **Cache injects the `mendeley_list_documents` callable from paper.py at construction time** — without this, monkeypatching the import-site alias misses the cache (cache holds its own ref to `docent.tools.mendeley_client.list_documents`); test failure caught this. 20 new tests (`test_mendeley_cache.py`: 11; `test_reader_overlay.py`: 8; `test_sync_from_mendeley.py`: +1 invalidation test). Suite 117 → 137 green in ~20s. **Real-data validated 2026-05-02 — overlay correctness confirmed (sync-from-mendeley populates queue from Docent-Queue collection; readers display fresh Mendeley metadata).** **Performance bug found in same validation:** every reader call still takes ~5s even when `list_documents` is a cache hit, because `_load_mendeley_overlay` ALWAYS calls `mendeley_list_folders` to resolve `queue_collection` → `folder_id`, and that's a separate MCP subprocess. The test `test_second_reader_call_hits_cache_not_mcp` even asserts `folder_calls == 2` — I dismissed it as "cheap" without thinking. **Fix queued for next session (11.7-followup):** cache `(collection_name → folder_id)` with a long TTL (folder ids are effectively static) in the same `mendeley_collection.json` file. ~10-line patch.
- [x] 11.7-followup. Folder-id cache landed. `MendeleyCache` gained a `list_folders` injection slot + `get_folder_id(collection_name)` method; folder map persists under reserved `"__folders__"` key in the same `mendeley_collection.json` with a 24h TTL. Duplicate names drop entirely (matches verbose sync-side semantics). `_resolve_collection_folder_id_quiet` collapsed to a one-line delegate. `test_second_reader_call_hits_cache_not_mcp` assertion flipped 2→1; 6 new `get_folder_id` tests. Suite 137 → 143 in ~18s. **Real-data validated 2026-05-02:** warm `paper next` now sub-second (was ~5s); cold pays one folders + one docs round-trip then stays warm for the TTL window.
- [x] 11.8. Rip out homegrown extraction. **Shipped 2026-05-02.** Deleted `paper_metadata.py` (~296 LOC: `resolve_metadata`, `extract_pdf_metadata`, `_extract_title_by_font_size`, `crossref_lookup`, `crossref_title_search`, `_filename_heuristic`) + `tests/test_metadata_resolver.py` (14 tests). Dropped `pypdf` from `pyproject.toml`; verified `pypdf` not in `sys.modules` after `import docent.cli`. `paper add` collapsed to two modes: bare `add` returns guidance ("Drop in database_dir → drag into Docent-Queue → sync-from-mendeley"); `add --mendeley-id <id>` upserts a stub sidecar entry keyed on the id (title/authors/year/doi pulled fresh from Mendeley overlay on next read). `AddInputs` slimmed to `mendeley_id`/`priority`/`course`/`notes`/`force` (dropped `title`/`authors`/`year`/`doi`/`pdf`); `AddResult.metadata_source` dropped. `scan` action + `ScanInputs`/`ScanResult` + `_resolve_scan_folder`/`_scan_folder` ripped (folder-walk + per-PDF add no longer makes sense once Mendeley owns ingestion). **Test fixture pivot:** ~30 sites across 6 test files used `tool.add(AddInputs(title=..., pdf=...))` as a fixture-builder; rewrote them to a new `seed_queue_entry` pytest fixture (in `conftest.py`) that builds `QueueEntry` directly and persists via `_store.save_queue` — tests no longer couple to a CLI surface they don't care about. New `tests/test_queue.py` (5 tests) covers the new add semantics: guidance shim, mendeley-id upsert, collision-blocked-without-force, force-update, whitespace-strip. Suite 143 → 128 green in ~3.6s. Reminded user to `uv tool install --reinstall --editable .` (done).
- [x] 11.9. Collapse `mendeley_watch_subdir`; retire `sync-promote` and `sync-mendeley` (subsumed by `sync-from-mendeley`). **Shipped 2026-05-02 (commit `6e6a155`).** `database_dir` IS the Mendeley watch folder now. Removed: `paper.mendeley_watch_subdir` setting + config-show/set paths + ConfigShowResult fields; `sync-promote` action + `SyncPromote{Inputs,Result}` + `paper_sync.move_to_watch` + `_path_in_watch`; `sync-mendeley` action + `SyncMendeley{Inputs,Result}` + `mendeley_client.lookup_doi`/`search_library`/`_normalize_doi`. MCP harness preserved (`list_folders` / `list_documents` still feed the read-through cache). `sync-status` simplified — dropped `watch_dir` / `promotable` / `in_watch` fields. Deleted `tests/test_sync_promote.py` + `tests/test_sync_mendeley.py`. `QueueEntry` schema fields (`pdf_path`, `file_status`, `keep_in_mendeley`, `promoted_at`, `title_is_filename_stub`) intentionally kept for 11.10 to delete in a single migration diff. Suite 128 → 102 green; net diff −1074 / +28 LOC.
- [x] 11.10. One-shot sidecar migration + schema trim. **Shipped 2026-05-02 (commit `b7d5e77`).** New `paper migrate-to-mendeley-truth [--yes]` action: backs up `queue.json` → `queue.json.bak` and wipes to the new shape. `QueueEntry` lost `pdf_path` / `file_status` / `keep_in_mendeley` / `promoted_at` / `title_is_filename_stub`; gained `started` / `finished` ISO timestamps stamped on first transition to `reading` / `done` (no overwrite). `mark-keeping` action removed (`keep_in_mendeley` field gone); `StatsResult.keeping` dropped; `BannerCounts.watch_files` dropped; `queue-index.json` no longer carries `file_status`. `_require_identifier` validator: `mendeley_id` or `doi` (pdf_path field gone). `sync-status` collapsed to `{database_dir, queue_size, database_pdfs}`. `sync-pull` no longer mutates the queue — downloads to `database_dir/{eid}.pdf` and trusts Mendeley to ingest; "already_has_file" keys on filename existence. Two new test files (`test_migrate_to_mendeley_truth.py`: 3, `test_status_timestamps.py`: 3); rewrote `test_sync_status.py` + parts of `test_sync_pull.py` for the new shape. `seed_queue_entry` conftest fixture lost dropped params; defaults `mendeley_id="m-<id>"` so DOI-less seeds satisfy validator. **Real-data validated 2026-05-02:** user ran `paper migrate-to-mendeley-truth --yes` against their live queue — clean. Suite 101 → 107 green in ~3.6s.
- [x] 11.R. Reading tool rewrite. **Shipped 2026-05-06.** `paper.py` (1229 LOC) → `reading.py`; `paper_store.py` → `reading_store.py`; `paper_sync.py` deleted (sync-pull removed). `PaperPipeline` → `ReadingQueue`, `name="reading"`, `category="reading"`. Settings: `PaperSettings` → `ReadingSettings`, `Settings.paper` → `Settings.reading`, config section `[paper]` → `[reading]`. Schema: `priority:str` → `order:int` (1-based queue position); `course:str|None` → `category:str` (course|thesis|personal) + `course_name:str|None`; `+deadline:str|None`. Actions removed: `migrate-to-mendeley-truth`, `sync-pull`. Actions added: `move-up`, `move-down`, `move-to`. Actions fixed: `edit` (no Mendeley-owned fields); `export` (applies overlay); `next` (sorts by order, filters by course_name); `stats` (by_category, by_course_name); `ready_to_read` → `start`. `BannerCounts` trimmed (removed db_files, mendeley_linked — always zero). New `reading_notify.py` — deadline check at startup, daily dedup. Hooked into `cli.py` `main` callback. Conftest updated (priority/course → order/category/course_name). Suite 107 → 93 green (−14: deleted test_migrate + test_sync_pull, added test_add_assigns_sequential_order). **Config migration required:** user must run `docent reading config-set database_dir <path>` once (old `[paper]` section is ignored).
- [ ] 12. External `~/.docent/plugins/` discovery
- [ ] 13. Full MCP adapter

## Where to look for detail

- Architectural decisions + rationale → `decisions.md` (append-only)
- Landmines we've hit → `gotchas.md`
- Paper-pipeline port plan → `archive/paper-pipeline-port-plan-2026-05.md` (archived; all steps shipped)
- User preferences → `feedback_*.md`

## Sanity check before trusting this file

Run `uv run docent --version` and glance at `src/docent/`. Make sure the checkboxes match reality before acting. Memory lies; code doesn't.

## Deferred / explicitly out-of-scope

- `utils/logging.py` — defer until a step needs logged output
- `ui/renderers.py` — defer until there's a second tool whose result render shape justifies reusable panels
- MCP adapter — build last, once the native registry is battle-tested
