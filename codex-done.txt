
All five pre-v1.2.0 release blockers are now cleared.

What changed in this pass:

Removed next/font/google from layout.tsx and switched globals.css to system font stacks.
Fixed stale flags in README.md, docs/cli.md, and the UI docs page.
Also hardened the remaining Next API subprocess calls in version and tooling so all frontend/src/app/api/** routes now avoid shell: true.
Updated CONTEXT.md and project_codex_review_blockers.md after the task.
Verification passed:

npm run lint
npx tsc --noEmit
python scripts/build_ui.py
uv run pytest --tb=no -q
Grep check for stale flags / next/font / shell: true
Small note: direct npm run build still hits the known Next static-export/API-route conflict. The repo’s intended packaged build path is python scripts/build_ui.py, which temporarily hides API routes and passed cleanly.

Next gate is not code: full real-life UI testing and the pending research real-life tests before any v1.2.0 tag/release